{
  "name": "copyskills",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "bun run --filter '*' build",
    "test": "bun run --filter '*' test",
    "dev:mcp": "bun run --filter @copydoc/mcp dev",
    "dev:cli": "bun run --filter @copydoc/cli dev",
    "typecheck": "bun run --filter '*' typecheck"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11",
    "@types/node": "^25.5.2",
    "typescript": "^6.0.2"
  }
}
